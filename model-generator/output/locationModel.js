/**
 * Model: LOCATIONS
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:55.213Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js LOCATIONS
 */

const LocationsModel = {
  tableName:    'LOCATIONS',
  sequenceName: 'locations_seq',

  fields: {
    location_id: { type: 'NUMBER', primaryKey: true },
    street_address: { type: 'VARCHAR2', maxLength: 65 },
    postal_code: { type: 'VARCHAR2', maxLength: 12 },
    city: { type: 'VARCHAR2', required: true, maxLength: 30 },
    state_province: { type: 'VARCHAR2', maxLength: 25 },
    country_id: { type: 'CHAR', required: true, maxLength: 2 },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    else if (data.street_address && String(data.street_address).length > 65) {
      errors.push('street_address maksimal 65 karakter');
    }
    else if (data.postal_code && String(data.postal_code).length > 12) {
      errors.push('postal_code maksimal 12 karakter');
    }
    if (data.city === undefined || data.city === null || String(data.city).trim() === '') {
      errors.push('city wajib diisi');
    }
    else if (data.city && String(data.city).length > 30) {
      errors.push('city maksimal 30 karakter');
    }
    else if (data.state_province && String(data.state_province).length > 25) {
      errors.push('state_province maksimal 25 karakter');
    }
    if (data.country_id === undefined || data.country_id === null || String(data.country_id).trim() === '') {
      errors.push('country_id wajib diisi');
    }
    else if (data.country_id && String(data.country_id).length > 2) {
      errors.push('country_id maksimal 2 karakter');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = LocationsModel;
