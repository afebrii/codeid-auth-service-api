/**
 * Model: BUSINESS_TRIP
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:47.580Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js BUSINESS_TRIP
 */

const BusinessTripModel = {
  tableName:    'BUSINESS_TRIP',
  sequenceName: 'business_trip_seq',

  fields: {
    trip_id: { type: 'NUMBER', primaryKey: true },
    employee_id: { type: 'NUMBER', required: true },
    destination: { type: 'VARCHAR2', required: true, maxLength: 100 },
    start_date: { type: 'DATE', required: true },
    end_date: { type: 'DATE', required: true },
    purpose: { type: 'VARCHAR2', maxLength: 255 },
    total_budget: { type: 'NUMBER', precision: 12, scale: 2 },
    status: { type: 'VARCHAR2', maxLength: 20 },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.employee_id === undefined || data.employee_id === null || String(data.employee_id).trim() === '') {
      errors.push('employee_id wajib diisi');
    }
    if (data.employee_id !== undefined && isNaN(Number(data.employee_id))) {
      errors.push('employee_id harus berupa angka');
    }
    if (data.destination === undefined || data.destination === null || String(data.destination).trim() === '') {
      errors.push('destination wajib diisi');
    }
    else if (data.destination && String(data.destination).length > 100) {
      errors.push('destination maksimal 100 karakter');
    }
    if (data.start_date === undefined || data.start_date === null || String(data.start_date).trim() === '') {
      errors.push('start_date wajib diisi');
    }
    if (data.end_date === undefined || data.end_date === null || String(data.end_date).trim() === '') {
      errors.push('end_date wajib diisi');
    }
    else if (data.purpose && String(data.purpose).length > 255) {
      errors.push('purpose maksimal 255 karakter');
    }
    else if (data.status && String(data.status).length > 20) {
      errors.push('status maksimal 20 karakter');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = BusinessTripModel;
