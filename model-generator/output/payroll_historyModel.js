/**
 * Model: PAYROLL_HISTORY
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:58.663Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js PAYROLL_HISTORY
 */

const PayrollHistoryModel = {
  tableName:    'PAYROLL_HISTORY',
  sequenceName: 'payroll_history_seq',

  fields: {
    payroll_id: { type: 'NUMBER', primaryKey: true },
    employee_id: { type: 'NUMBER', unique: true, required: true },
    period_month: { type: 'NUMBER', unique: true, required: true, precision: 2 },
    period_year: { type: 'NUMBER', unique: true, required: true, precision: 4 },
    base_salary: { type: 'NUMBER', required: true, precision: 12, scale: 2 },
    total_allowance: { type: 'NUMBER', precision: 12, scale: 2 },
    total_deduction: { type: 'NUMBER', precision: 12, scale: 2 },
    net_salary: { type: 'NUMBER', required: true, precision: 12, scale: 2 },
    payment_date: { type: 'DATE' },
    status: { type: 'VARCHAR2', maxLength: 20 },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.employee_id === undefined || data.employee_id === null || String(data.employee_id).trim() === '') {
      errors.push('employee_id wajib diisi');
    }
    if (data.employee_id !== undefined && isNaN(Number(data.employee_id))) {
      errors.push('employee_id harus berupa angka');
    }
    if (data.period_month === undefined || data.period_month === null || String(data.period_month).trim() === '') {
      errors.push('period_month wajib diisi');
    }
    if (data.period_month !== undefined && isNaN(Number(data.period_month))) {
      errors.push('period_month harus berupa angka');
    }
    if (data.period_year === undefined || data.period_year === null || String(data.period_year).trim() === '') {
      errors.push('period_year wajib diisi');
    }
    if (data.period_year !== undefined && isNaN(Number(data.period_year))) {
      errors.push('period_year harus berupa angka');
    }
    if (data.base_salary === undefined || data.base_salary === null || String(data.base_salary).trim() === '') {
      errors.push('base_salary wajib diisi');
    }
    if (data.base_salary !== undefined && isNaN(Number(data.base_salary))) {
      errors.push('base_salary harus berupa angka');
    }
    if (data.net_salary === undefined || data.net_salary === null || String(data.net_salary).trim() === '') {
      errors.push('net_salary wajib diisi');
    }
    if (data.net_salary !== undefined && isNaN(Number(data.net_salary))) {
      errors.push('net_salary harus berupa angka');
    }
    else if (data.status && String(data.status).length > 20) {
      errors.push('status maksimal 20 karakter');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = PayrollHistoryModel;
