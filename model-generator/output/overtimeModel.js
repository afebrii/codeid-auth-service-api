/**
 * Model: OVERTIME
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:56.325Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js OVERTIME
 */

const OvertimeModel = {
  tableName:    'OVERTIME',
  sequenceName: 'overtime_seq',

  fields: {
    overtime_id: { type: 'NUMBER', primaryKey: true },
    employee_id: { type: 'NUMBER', required: true },
    overtime_date: { type: 'DATE', required: true },
    hours: { type: 'NUMBER', required: true, precision: 4, scale: 2 },
    reason: { type: 'VARCHAR2', maxLength: 255 },
    status: { type: 'VARCHAR2', maxLength: 20 },
    approved_by: { type: 'NUMBER' },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.employee_id === undefined || data.employee_id === null || String(data.employee_id).trim() === '') {
      errors.push('employee_id wajib diisi');
    }
    if (data.employee_id !== undefined && isNaN(Number(data.employee_id))) {
      errors.push('employee_id harus berupa angka');
    }
    if (data.overtime_date === undefined || data.overtime_date === null || String(data.overtime_date).trim() === '') {
      errors.push('overtime_date wajib diisi');
    }
    if (data.hours === undefined || data.hours === null || String(data.hours).trim() === '') {
      errors.push('hours wajib diisi');
    }
    if (data.hours !== undefined && isNaN(Number(data.hours))) {
      errors.push('hours harus berupa angka');
    }
    else if (data.reason && String(data.reason).length > 255) {
      errors.push('reason maksimal 255 karakter');
    }
    else if (data.status && String(data.status).length > 20) {
      errors.push('status maksimal 20 karakter');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = OvertimeModel;
