/**
 * Model: ATTENDANCES
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:45.430Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js ATTENDANCES
 */

const AttendancesModel = {
  tableName:    'ATTENDANCES',
  sequenceName: 'attendances_seq',

  fields: {
    attendance_id: { type: 'NUMBER', primaryKey: true },
    employee_id: { type: 'NUMBER', unique: true, required: true },
    attendance_date: { type: 'DATE', unique: true, required: true },
    check_in: { type: 'TIMESTAMP' },
    check_out: { type: 'TIMESTAMP' },
    status: { type: 'VARCHAR2', maxLength: 20 },
    notes: { type: 'VARCHAR2', maxLength: 255 },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.employee_id === undefined || data.employee_id === null || String(data.employee_id).trim() === '') {
      errors.push('employee_id wajib diisi');
    }
    if (data.employee_id !== undefined && isNaN(Number(data.employee_id))) {
      errors.push('employee_id harus berupa angka');
    }
    if (data.attendance_date === undefined || data.attendance_date === null || String(data.attendance_date).trim() === '') {
      errors.push('attendance_date wajib diisi');
    }
    else if (data.status && String(data.status).length > 20) {
      errors.push('status maksimal 20 karakter');
    }
    else if (data.notes && String(data.notes).length > 255) {
      errors.push('notes maksimal 255 karakter');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = AttendancesModel;
