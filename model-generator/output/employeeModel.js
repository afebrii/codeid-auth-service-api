/**
 * Model: EMPLOYEES
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:52.628Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js EMPLOYEES
 */

const EmployeesModel = {
  tableName:    'EMPLOYEES',
  sequenceName: 'employees_seq',

  fields: {
    employee_id: { type: 'NUMBER', primaryKey: true },
    first_name: { type: 'VARCHAR2', maxLength: 20 },
    last_name: { type: 'VARCHAR2', required: true, maxLength: 25 },
    email: { type: 'VARCHAR2', required: true, maxLength: 100 },
    phone_number: { type: 'VARCHAR2', maxLength: 20 },
    hire_date: { type: 'DATE', required: true },
    job_id: { type: 'NUMBER', required: true },
    salary: { type: 'NUMBER', required: true, precision: 8, scale: 2 },
    manager_id: { type: 'NUMBER' },
    department_id: { type: 'NUMBER' },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.first_name && String(data.first_name).length > 20) {
      errors.push('first_name maksimal 20 karakter');
    }
    if (data.last_name === undefined || data.last_name === null || String(data.last_name).trim() === '') {
      errors.push('last_name wajib diisi');
    }
    else if (data.last_name && String(data.last_name).length > 25) {
      errors.push('last_name maksimal 25 karakter');
    }
    if (data.email === undefined || data.email === null || String(data.email).trim() === '') {
      errors.push('email wajib diisi');
    }
    else if (data.email && String(data.email).length > 100) {
      errors.push('email maksimal 100 karakter');
    }
    else if (data.phone_number && String(data.phone_number).length > 20) {
      errors.push('phone_number maksimal 20 karakter');
    }
    if (data.hire_date === undefined || data.hire_date === null || String(data.hire_date).trim() === '') {
      errors.push('hire_date wajib diisi');
    }
    if (data.job_id === undefined || data.job_id === null || String(data.job_id).trim() === '') {
      errors.push('job_id wajib diisi');
    }
    if (data.job_id !== undefined && isNaN(Number(data.job_id))) {
      errors.push('job_id harus berupa angka');
    }
    if (data.salary === undefined || data.salary === null || String(data.salary).trim() === '') {
      errors.push('salary wajib diisi');
    }
    if (data.salary !== undefined && isNaN(Number(data.salary))) {
      errors.push('salary harus berupa angka');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = EmployeesModel;
