/**
 * Model: DEPENDENTS
 * Auto-generated by Oracle Model Generator
 * Generated at: 2026-05-27T17:20:51.549Z
 *
 * DO NOT EDIT — regenerate dengan: node generate-models.js DEPENDENTS
 */

const DependentsModel = {
  tableName:    'DEPENDENTS',
  sequenceName: 'dependents_seq',

  fields: {
    dependent_id: { type: 'NUMBER', primaryKey: true },
    first_name: { type: 'VARCHAR2', required: true, maxLength: 50 },
    last_name: { type: 'VARCHAR2', required: true, maxLength: 50 },
    relationship: { type: 'VARCHAR2', required: true, maxLength: 25 },
    employee_id: { type: 'NUMBER', required: true },
  },

  /**
   * Validasi payload input (CREATE / UPDATE)
   * @param {object} data
   * @returns {{ valid: boolean, errors: string[] }}
   */
  validate(data) {
    const errors = [];

    if (data.first_name === undefined || data.first_name === null || String(data.first_name).trim() === '') {
      errors.push('first_name wajib diisi');
    }
    else if (data.first_name && String(data.first_name).length > 50) {
      errors.push('first_name maksimal 50 karakter');
    }
    if (data.last_name === undefined || data.last_name === null || String(data.last_name).trim() === '') {
      errors.push('last_name wajib diisi');
    }
    else if (data.last_name && String(data.last_name).length > 50) {
      errors.push('last_name maksimal 50 karakter');
    }
    if (data.relationship === undefined || data.relationship === null || String(data.relationship).trim() === '') {
      errors.push('relationship wajib diisi');
    }
    else if (data.relationship && String(data.relationship).length > 25) {
      errors.push('relationship maksimal 25 karakter');
    }
    if (data.employee_id === undefined || data.employee_id === null || String(data.employee_id).trim() === '') {
      errors.push('employee_id wajib diisi');
    }
    if (data.employee_id !== undefined && isNaN(Number(data.employee_id))) {
      errors.push('employee_id harus berupa angka');
    }

    return { valid: errors.length === 0, errors };
  },
};

module.exports = DependentsModel;
